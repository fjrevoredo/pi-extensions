# macOS
.DS_Store

# Logs
*.log

# Node / package managers
# package-lock.json is tracked: it is what pins the 252 resolved packages that
# package.json's 7 direct pins leave floating (C2), and `npm ci` needs it.
node_modules/
pnpm-lock.yaml
yarn.lock

# Build / temporary outputs
dist/
build/
coverage/
*.tsbuildinfo
.tmp/
tmp/

# Local editor / tooling noise
.vscode/
.idea/
*.swp
*.swo
